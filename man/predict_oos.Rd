% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_oos}
\alias{predict_oos}
\title{Out-of-sample predictions from a fitted SEM}
\usage{
predict_oos(
  params,
  newdata,
  ynames,
  xnames,
  construction = c("implied", "chain")
)
}
\arguments{
\item{params}{An object from \code{\link[=sem_params]{sem_params()}}.}

\item{newdata}{Data frame containing \code{xnames}.}

\item{ynames, xnames}{Character vectors of outcome and predictor indicator
names.}

\item{construction}{\code{"implied"} or \code{"chain"}.}
}
\value{
A numeric matrix, rows of \code{newdata} by \code{ynames}.
}
\description{
Turns a fitted model into case-level predictions of observed outcome
indicators from observed predictor indicators, by one of two
constructions.
}
\details{
\itemize{
\item \code{"implied"}: the model-implied regression (de Rooij et al., 2023), the
best linear prediction of \code{ynames} from \code{xnames} under the fitted mean
vector and covariance matrix, \verb{mu_y + Sigma_yx Sigma_xx^-1 (x - mu_x)}.
For lavaan models this equals \code{lavaan::lavPredictY()}. For PLSc models
the implied covariances are built from the corrected loadings and the
corrected construct correlations, so scores and parameters are in the
same metric; it is unavailable when the corrected solution is
inadmissible (see \code{\link[=sem_params]{sem_params()}}).
\item \code{"chain"}: the construct-score chain used by PLSpredict (Shmueli et al.,
2016, 2019): predictor items are weighted into construct scores, carried
through the path coefficients in causal order, and mapped to the outcome
items through the loadings. Only available for seminr models. Under
consistent PLS this construction applies factor-metric coefficients to
composite-metric scores and over-disperses the predictions.
}
}
\examples{
d <- lavaan::PoliticalDemocracy
fit <- lavaan::sem("ind60 =~ x1 + x2 + x3
                    dem60 =~ y1 + y2 + y3 + y4
                    dem65 =~ y5 + y6 + y7 + y8
                    dem60 ~ ind60
                    dem65 ~ ind60 + dem60", data = d[1:60, ], meanstructure = TRUE)
yhat <- predict_oos(sem_params(fit), d[61:75, ], ynames = paste0("y", 5:8),
                    xnames = c(paste0("x", 1:3), paste0("y", 1:4)))
head(yhat)
}
\references{
de Rooij, M., et al. (2023). SEM-based out-of-sample
predictions. \emph{Structural Equation Modeling}, 30(1), 132–148.
Shmueli, G., et al. (2016). The elephant in the room: Predictive
performance of PLS models. \emph{Journal of Business Research}, 69(10),
4552–4564.
}
