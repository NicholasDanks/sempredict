% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{cv_predict}
\alias{cv_predict}
\title{Repeated k-fold cross-validated prediction with identical folds}
\usage{
cv_predict(
  methods,
  data,
  ynames,
  xnames,
  k = 10,
  reps = 1,
  seed = NULL,
  benchmarks = c("mean", "lm"),
  verbose = FALSE
)
}
\arguments{
\item{methods}{Named list of \code{\link[=sem_method]{sem_method()}} objects.}

\item{data}{Data frame with all indicators.}

\item{ynames, xnames}{Outcome and predictor indicator names.}

\item{k}{Number of folds.}

\item{reps}{Number of repetitions (fresh random fold assignments).}

\item{seed}{Random seed for the fold assignments; set it so the run is
reproducible.}

\item{benchmarks}{Any of \code{"mean"} (the training-mean predictor) and \code{"lm"}
(a multivariate linear regression of \code{ynames} on \code{xnames}).}

\item{verbose}{Print progress.}
}
\value{
An object of class \code{sem_cv} holding the fold assignments, the
out-of-sample predictions and squared errors for every case, repetition
and method, the in-sample MSE per training fold, and the failure log.
Summarise it with \code{\link[=assess]{assess()}}, test it with \code{\link[=cvpat]{cvpat()}}, draw it with
\code{\link[=plot_dispersion]{plot_dispersion()}}.
}
\description{
Splits the data into \code{k} folds, \code{reps} times, and for every fold refits
every method on the training part and predicts \code{ynames} for the held-out
part from \code{xnames}. All methods and benchmarks see the same folds, so every
comparison is paired. Failures (non-convergence, inadmissible PLSc
solutions, prediction errors) are counted per method with their reasons
and never stop the run.
}
\examples{
d <- seminr::mobi
mm <- seminr::constructs(
  seminr::reflective("EXP", seminr::multi_items("CUEX", 1:3)),
  seminr::reflective("SAT", seminr::multi_items("CUSA", 1:3)))
sm <- seminr::relationships(seminr::paths(from = "EXP", to = "SAT"))
plsc <- sem_method(function(tr) seminr::estimate_pls(tr, mm, sm),
                   construction = c("chain", "implied"))
cv <- cv_predict(list(PLSc = plsc), d, ynames = paste0("CUSA", 1:3),
                 xnames = paste0("CUEX", 1:3), k = 10, reps = 2, seed = 1)
assess(cv)
}
