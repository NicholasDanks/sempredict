% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{sem_method}
\alias{sem_method}
\title{Define a method for cross-validated prediction}
\usage{
sem_method(fit, construction = "implied", label = NULL, params_args = list())
}
\arguments{
\item{fit}{A function of one argument, the training data frame, returning
a fitted \code{lavaan} or \code{seminr_model} object. Errors and non-convergence
are caught and counted as failures.}

\item{construction}{Character vector, any of \code{"implied"} and \code{"chain"}.
With two constructions the method contributes two result columns,
\code{label_implied} and \code{label_chain}, from one fit per fold.}

\item{label}{Name used in results; defaults to the name given in the
\code{methods} list of \code{\link[=cv_predict]{cv_predict()}}.}

\item{params_args}{List of extra arguments to \code{\link[=sem_params]{sem_params()}} (for example
\code{list(plsc = TRUE)}).}
}
\value{
An object of class \code{sem_method}.
}
\description{
A method is a fitting function plus one or more prediction constructions.
\code{\link[=cv_predict]{cv_predict()}} refits it on every training fold and predicts the held-out
fold by each construction.
}
\examples{
m <- sem_method(function(d) lavaan::sem("f =~ x1 + x2 + x3", data = d,
                                         meanstructure = TRUE))
}
