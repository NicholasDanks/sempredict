% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{sem_params}
\alias{sem_params}
\alias{sem_params.lavaan}
\alias{sem_params.seminr_model}
\title{Prediction parameters of a fitted SEM}
\usage{
sem_params(fit, ...)

\method{sem_params}{lavaan}(fit, ...)

\method{sem_params}{seminr_model}(fit, plsc = NULL, ...)
}
\arguments{
\item{fit}{A fitted \code{lavaan} object (single group, fitted with
\code{meanstructure = TRUE}) or a \code{seminr_model}.}

\item{...}{Passed to methods.}

\item{plsc}{Logical; treat the model as consistent PLS. Defaults to
\code{TRUE} when any construct was specified with \code{seminr::reflective()}.
Composite constructs in a mixed model are given rho_A = 1.}
}
\value{
An object of class \code{sem_params}: a list with \code{estimator},
\code{Sigma} and \code{mu} (model-implied moments on the raw indicator scale,
\code{NULL} when inadmissible), \code{sd} (training standard deviations), \code{chain}
(\code{NULL} for lavaan), \code{rhoA}, \code{admissible} (logical) and \code{reason}.
}
\description{
Extracts from a fitted model what the two prediction constructions need:
the model-implied indicator mean vector and covariance matrix (for the
model-implied regression) and, for \code{seminr} models, the outer weights, path
coefficients and loadings (for the construct-score chain). For consistent
PLS (constructs specified with \code{seminr::reflective()}) the construct
correlations are corrected for unreliability with each construct's
rho_A, propagated through the structural model, and checked for
admissibility: a corrected correlation of absolute value one or more, a
non-positive-definite construct correlation matrix, or a rho_A outside
(0, 1] makes the model-implied regression unavailable. The chain is
always available because it never forms the implied covariances.
}
\examples{
fit <- lavaan::sem("ind60 =~ x1 + x2 + x3
                    dem60 =~ y1 + y2 + y3 + y4
                    dem65 =~ y5 + y6 + y7 + y8
                    dem60 ~ ind60
                    dem65 ~ ind60 + dem60",
                   data = lavaan::PoliticalDemocracy, meanstructure = TRUE)
p <- sem_params(fit)
p$admissible
}
